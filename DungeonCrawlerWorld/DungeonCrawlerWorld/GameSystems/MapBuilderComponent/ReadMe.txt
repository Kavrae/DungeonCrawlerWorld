MapBuilderComponent
	Used to generate maps.
	Should NOT hold definitions for the map objects or the components that grant them behavior.
	SHOULD contain the logic to use those objects and apply the components to them.