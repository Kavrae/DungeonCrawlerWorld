using Microsoft.Xna.Framework;

namespace Presentation.UI;

/// <summary>Generic content-area appearance, independent of what a window's content actually is.</summary>
public sealed class WindowContentOptions
{
    public Color? ContentColor { get; set; }
}